import { CardsList } from '../cards-list';

const Main = () => {
  return (
    <main className="results container">
      <h1>Results</h1>
      <CardsList></CardsList>
    </main>
  );
};

export { Main };
